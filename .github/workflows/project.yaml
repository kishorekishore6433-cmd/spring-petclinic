on:
  push:
    branches: [main]

jobs:
  maven:
      runs-on: [ubuntu-24.04]
      steps:
        - name: checkout the code
          uses: actions/checkout@v4
        - name: biuld code  
          run: mvn package
        
         